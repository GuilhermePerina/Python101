import math
n=int(input('Escolha um número: '))
raiz=math.sqrt(n)

print('A raiz de {} é {}'.format(n, math.ceil(raiz)))

from math import sqrt
n1=int(input('Escolha outro número: '))
raiz2=sqrt(n1)
print('A raiz de {} é {:.2f}'.format(n1, raiz2))
